# Production Environment Configuration for Influmojo Webapp
# This file should be loaded from AWS Parameter Store in production
# Configured for ALB deployment (no direct port exposure)

# API Configuration
NEXT_PUBLIC_API_URL=https://api.influmojo.com

# Google OAuth
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here

# App Configuration
NODE_ENV=production

# Port Configuration (for EC2 internal communication with ALB)
PORT=3000

# CORS Configuration
CORS_ORIGINS=https://influmojo.com,https://www.influmojo.com,https://api.influmojo.com

# Security
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=https://influmojo.com

# ALB Configuration Notes:
# - Webapp runs on port 3000 (internal only)
# - ALB forwards traffic from influmojo.com to port 3000
# - No direct external access to port 3000
